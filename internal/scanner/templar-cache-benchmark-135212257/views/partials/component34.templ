package components

import (
	"fmt"
	"strings"
)

templ Input34(props map[string]interface{}, classes []string) {
	<div class="input">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Input component 34</p>
	</div>
}

templ Input134(props map[string]interface{}, classes []string) {
	<div class="input">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Input component 34</p>
	</div>
}

