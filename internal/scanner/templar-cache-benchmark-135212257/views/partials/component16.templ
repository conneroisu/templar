package components

import (
	"fmt"
	"strings"
	"context"
)

templ Table16(title string) {
	<div class="table">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Table component 16</p>
	</div>
}

templ Table116(title string) {
	<div class="table">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Table component 16</p>
	</div>
}

