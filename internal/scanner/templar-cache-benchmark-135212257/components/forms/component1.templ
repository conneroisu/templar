package components

import (
	"fmt"
	"strings"
)

templ Card1(title string, disabled bool) {
	<div class="card">
		<p>This is Card component 1</p>
	</div>
}

templ Card11(title string, disabled bool) {
	<div class="card">
		<p>This is Card component 1</p>
	</div>
}

