package components

import (
	"fmt"
	"strings"
)

templ Header37(title string, disabled bool) {
	<div class="header">
		<p>This is Header component 37</p>
	</div>
}

templ Header137(title string, disabled bool) {
	<div class="header">
		<p>This is Header component 37</p>
	</div>
}

