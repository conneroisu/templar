package components

import (
	"fmt"
	"strings"
)

templ Form13(title string, disabled bool) {
	<div class="form">
		<p>This is Form component 13</p>
	</div>
}

templ Form113(title string, disabled bool) {
	<div class="form">
		<p>This is Form component 13</p>
	</div>
}

