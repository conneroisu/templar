package components

import (
	"fmt"
	"strings"
)

templ Select25(title string, disabled bool) {
	<div class="select">
		<p>This is Select component 25</p>
		@Icon1("check")
	</div>
}

templ Select125(title string, disabled bool) {
	<div class="select">
		<p>This is Select component 25</p>
		@Icon1("check")
	</div>
}

