package components

import (
	"fmt"
	"strings"
	"time"
	"context"
)

templ Input24(title string) {
	<div class="input">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Input component 24</p>
	</div>
}

