package components

import (
	"fmt"
	"strings"
	"time"
	"context"
)

templ Table36(title string) {
	<div class="table">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Table component 36</p>
	</div>
}

