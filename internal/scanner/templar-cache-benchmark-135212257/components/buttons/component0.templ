package components

import (
	"fmt"
	"strings"
	"time"
	"context"
)

templ Button0(title string) {
	<div class="button">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Button component 0</p>
		@Icon0("check")
	</div>
}

