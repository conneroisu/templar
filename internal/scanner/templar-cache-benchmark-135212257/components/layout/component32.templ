package components

import (
	"fmt"
	"strings"
	"context"
)

templ Modal32(title string) {
	<div class="modal">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Modal component 32</p>
	</div>
}

templ Modal132(title string) {
	<div class="modal">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Modal component 32</p>
	</div>
}

templ Modal232(title string) {
	<div class="modal">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Modal component 32</p>
	</div>
}

