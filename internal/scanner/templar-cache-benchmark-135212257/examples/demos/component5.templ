package components

import (
	"fmt"
	"strings"
)

templ Select5(title string, disabled bool) {
	<div class="select">
		<p>This is Select component 5</p>
		@Icon2("check")
	</div>
}

templ Select15(title string, disabled bool) {
	<div class="select">
		<p>This is Select component 5</p>
		@Icon2("check")
	</div>
}

templ Select25(title string, disabled bool) {
	<div class="select">
		<p>This is Select component 5</p>
		@Icon2("check")
	</div>
}

