package components

import (
	"fmt"
	"strings"
)

templ Card41(title string, disabled bool) {
	<div class="card">
		<p>This is Card component 41</p>
	</div>
}

templ Card141(title string, disabled bool) {
	<div class="card">
		<p>This is Card component 41</p>
	</div>
}

templ Card241(title string, disabled bool) {
	<div class="card">
		<p>This is Card component 41</p>
	</div>
}

