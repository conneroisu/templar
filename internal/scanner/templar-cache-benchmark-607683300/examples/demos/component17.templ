package components

import (
	"fmt"
	"strings"
)

templ Header17(title string, disabled bool) {
	<div class="header">
		<p>This is Header component 17</p>
	</div>
}

templ Header117(title string, disabled bool) {
	<div class="header">
		<p>This is Header component 17</p>
	</div>
}

templ Header217(title string, disabled bool) {
	<div class="header">
		<p>This is Header component 17</p>
	</div>
}

