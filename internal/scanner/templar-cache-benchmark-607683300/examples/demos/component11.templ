package components

import (
	"fmt"
	"strings"
)

templ Card11(ctx context.Context, data interface{}, opts ...Option) {
	<div class="card">
		<p>This is Card component 11</p>
	</div>
}

templ Card111(ctx context.Context, data interface{}, opts ...Option) {
	<div class="card">
		<p>This is Card component 11</p>
	</div>
}

templ Card211(ctx context.Context, data interface{}, opts ...Option) {
	<div class="card">
		<p>This is Card component 11</p>
	</div>
}

