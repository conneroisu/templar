package components

import (
	"fmt"
	"strings"
)

templ Form23(ctx context.Context, data interface{}, opts ...Option) {
	<div class="form">
		<p>This is Form component 23</p>
	</div>
}

templ Form123(ctx context.Context, data interface{}, opts ...Option) {
	<div class="form">
		<p>This is Form component 23</p>
	</div>
}

templ Form223(ctx context.Context, data interface{}, opts ...Option) {
	<div class="form">
		<p>This is Form component 23</p>
	</div>
}

