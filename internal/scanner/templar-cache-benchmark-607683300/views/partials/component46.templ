package components

import (
	"fmt"
	"strings"
)

templ Table46(props map[string]interface{}, classes []string) {
	<div class="table">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Table component 46</p>
	</div>
}

templ Table146(props map[string]interface{}, classes []string) {
	<div class="table">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Table component 46</p>
	</div>
}

