package components

import (
	"fmt"
	"strings"
	"context"
)

templ Input4(title string) {
	<div class="input">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Input component 4</p>
	</div>
}

templ Input14(title string) {
	<div class="input">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Input component 4</p>
	</div>
}

