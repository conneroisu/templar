package components

import (
	"fmt"
	"strings"
	"context"
)

templ Footer28(title string) {
	<div class="footer">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Footer component 28</p>
	</div>
}

templ Footer128(title string) {
	<div class="footer">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Footer component 28</p>
	</div>
}

