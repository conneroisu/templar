package components

import (
	"fmt"
	"strings"
)

templ Modal22(props map[string]interface{}, classes []string) {
	<div class="modal">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Modal component 22</p>
	</div>
}

templ Modal122(props map[string]interface{}, classes []string) {
	<div class="modal">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Modal component 22</p>
	</div>
}

