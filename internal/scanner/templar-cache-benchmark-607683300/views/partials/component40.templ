package components

import (
	"fmt"
	"strings"
	"context"
)

templ Button40(title string) {
	<div class="button">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Button component 40</p>
		@Icon1("check")
	</div>
}

templ Button140(title string) {
	<div class="button">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Button component 40</p>
		@Icon1("check")
	</div>
}

