package components

import (
	"fmt"
	"strings"
	"time"
	"context"
)

templ Modal12(title string) {
	<div class="modal">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Modal component 12</p>
	</div>
}

