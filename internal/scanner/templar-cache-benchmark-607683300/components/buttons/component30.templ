package components

import (
	"fmt"
	"strings"
	"time"
)

templ Button30(props map[string]interface{}, classes []string) {
	<div class="button">
		if title != "" {
			<h2>{ title }</h2>
		}
		<p>This is Button component 30</p>
		@Icon0("check")
	</div>
}

