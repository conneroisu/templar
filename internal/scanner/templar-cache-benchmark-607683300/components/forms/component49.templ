package components

import (
	"fmt"
	"strings"
)

templ Sidebar49(title string, disabled bool) {
	<div class="sidebar">
		<p>This is Sidebar component 49</p>
	</div>
}

templ Sidebar149(title string, disabled bool) {
	<div class="sidebar">
		<p>This is Sidebar component 49</p>
	</div>
}

