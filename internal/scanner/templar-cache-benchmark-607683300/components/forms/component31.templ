package components

import (
	"fmt"
	"strings"
)

templ Card31(ctx context.Context, data interface{}, opts ...Option) {
	<div class="card">
		<p>This is Card component 31</p>
	</div>
}

templ Card131(ctx context.Context, data interface{}, opts ...Option) {
	<div class="card">
		<p>This is Card component 31</p>
	</div>
}

